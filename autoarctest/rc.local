#!/bin/sh

LOGDIR=/var/log/autoarctest
mkdir -p "$LOGDIR"
sleep 30
/home/jaadams/arctest/autoarctest/autotest.py \
	> "$LOGDIR/$(date --iso-8601=seconds).log" 2>&1 \
	|| sendmessage.sh "$(printf "Test Run Error:\n%s" \
		"$(tail "$LOGDIR/$(ls "$LOGDIR" | sort | tail -1)")")"
